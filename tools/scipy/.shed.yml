name: scipy
owner: recetox
remote_repository_url: "https://github.com/RECETOX/galaxytools/tree/master/tools/scipy"
homepage_url: "https://docs.scipy.org/doc/scipy/tutorial/interpolate.html"
categories:
  - Metabolomics
  - Statistics
description: "Searching, filtering and converting mass spectral libraries."
long_description: "Tools to filter (normalize intensities, filter mz range, convert adducts, ...) and convert (msp/mgf/json) mass spectral libraries and to perform spectral, fingerprint and metadata similarity calculations as well as molecular networking."
auto_tool_repositories:
  name_template: "{{ tool_id }}"
  description_template: "{{ tool_name }} tool from the scipy package"
suite:
  name: suite_scipy
  description: tools from the scipy suite are used for science
  type: repository_suite_definition