<tool id="peaktable_plot_aligned" name="plot aligned peaktable" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="20.01" license="MIT">
    <description></description>
    <macros>
        <token name="@TOOL_VERSION@">5.24.1</token>
        <token name="@VERSION_SUFFIX@">0</token>
    </macros>
    <!-- TODO: please annotate this tool with topics and operations from http://edamontology.org -->
    <!-- TODO: for more information see: https://galaxy-iuc-standards.readthedocs.io/en/latest/best_practices/tool_xml.html#edam-topics-and-operations -->
    <!-- <edam_topics>
        <edam_topic>topic_TODO</edam_topic>
    </edam_topics>
    <edam_operations>
        <edam_operation>operation_TODO</edam_operation>
    </edam_operations> -->
    <!-- <xrefs>
        <xref type="bio.tools"></xref>
    </xrefs> -->
    <requirements>
        <requirement type="package" version="@TOOL_VERSION@">plotly</requirement>
        <requirement type="package" version="23.0.1">pyarrow</requirement>
        <requirement type="package" version="5.0.1">pyteomics</requirement>
        <requirement type="package" version="1.3.5">psims</requirement>
        <requirement type="package" version="3.1.6">jinja2</requirement>
    </requirements>
    <command detect_errors="exit_code"><![CDATA[
        mkdir -p 'files' &&
        #for $f in $input_files:
            ln -s '${f}' 'files/${f.element_identifier}.${f.ext}' &&
        #end for
        ln -s '${annotation_path}' '${annotation_path.name}.${annotation_path.ext}' &&
        ln -s '${markers_path}' '${markers_path.name}.${markers_path.ext}' &&
        python3 '${__tool_directory__}/peaktable_plot_aligned.py'
            --title 'Title'
            --raw-input-path 'files'
            --noise-threshold $noise_threshold
            --output-html '$plot'
            --cores-to-use \${GALAXY_SLOTS:-1}
            ${show_only_annotated}
            --markers-path '${markers_path.name}.${markers_path.ext}'
            --intensity-path '${intensity_path}'
            --rt-path '${rt_path}'
            --metadata-path '${metadata_path}'
            --annotation-path '${annotation_path.name}.${annotation_path.ext}'
            --mz-tol-ppm ${mz_tol_ppm}
            --rt-tol-s ${rt_tol_s}
    ]]></command>
    <inputs>
        <param name="input_files" type="data_collection" format="mzml" collection_type="list" label="Input files" help="TODO" />
        <param name="noise_threshold" type="integer" min="0" value="1000" label="Noise threshold" help="TODO" />
        <param argument="--show-only-annotated" type="boolean" truevalue="--show-only-annotated" falsevalue="" checked="false" label="Show only annotated features" help="TODO" />
        <param argument="--markers-path" type="data" format="tsv,tabular" label="Markers / transition list" help="TODO" />
        <param argument="--intensity-path" type="data" format="parquet,csv" label="Aligned intensity table / data matrix" help="TODO" />
        <param argument="--rt-path" type="data" format="parquet,csv" label="Aligned retention time table" help="TODO" />
        <param argument="--metadata-path" type="data" format="parquet,csv" label="Variable metadata table" help="TODO" />
        <param argument="--annotation-path" type="data" format="tabular,tsv,csv" label="Annotation table" help="TODO" />
        <param argument="--mz-tol-ppm" type="integer" min="0" max="100" value="5" label="m/z tol [ppm]" help="TODO" />
        <param argument="--rt-tol-s" type="float" min="0" max="100" value="10" label="rt tol [s]" help="TODO" />
    </inputs>
    <outputs>
        <data name="plot" format="html" label="${tool.name} on ${on_string}" />
    </outputs>
    <tests>
        <!-- Hint: You can use [ctrl+alt+t] after defining the inputs/outputs to auto-scaffold some basic test cases. -->
    </tests>
    <help><![CDATA[

.. class:: infomark

**What it does**

TODO: Fill in help in reStructuredText format (https://docutils.sourceforge.io/docs/ref/rst/restructuredtext.html)

Usage
.....


**Input**


**Output**


    ]]></help>
    <citations>
        <citation type="doi">10.1007/s13361-012-0516-6</citation>
        <citation type="doi">10.5281/zenodo.14503524</citation>
    </citations>
</tool>