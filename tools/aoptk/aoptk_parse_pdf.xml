<tool id="aoptk_parse_pdf" name="aoptk parse PDF" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="25.1" license="MIT">
    <description>Parse PDF files.</description>
    <macros>
        <import>macros.xml</import>
    </macros>

    <requirements>
        <expand macro="requirements"/>
        <expand macro="openai_config"/>
    </requirements>
 
    <command detect_errors="exit_code"><![CDATA[
        python3 '${parse_pdf}'
    ]]></command>

<configfiles>
<configfile name="parse_pdf">
import os
import sys

import yaml
from aoptk.literature.pdf import PDF
from aoptk.literature.pymupdf_parser import PymupdfParser
from aoptk.text_generation_api import TextGenerationAPI

publication_id = os.path.splitext("$input_file.element_identifier")[0]

#if str($use_llms) == "true"
litellm_api_key = os.environ.get("OPENAI_API_KEY")
openai_endpoint_url = os.environ.get("OPENAI_ENDPOINT_URL") or "https://llm.ai.e-infra.cz/v1"
text_generation=TextGenerationAPI(model="qwen3.5", api_key=litellm_api_key, url=openai_endpoint_url)

#else
text_generation = None
#end if

text = PymupdfParser([PDF("$input_file")], text_generation=text_generation).get_publications(download_figures_enabled=False)[0].full_text
with open(f"{publication_id}.txt", "w") as f:
    f.write(text)

</configfile>
</configfiles>

    <inputs>
        <param name="input_file" type="data" format="pdf" label="PDF to parse" help="Input PDF." />
        <param name="use_llms" type="boolean" checked="false" label="Use LLMs to parse scanned PDFs" help="Check if you want to use LLMs to parse old PDFs which contain scans of text as images and not text encoded through OCR." />
    </inputs>

    <outputs>
        <data name="text" format="txt" from_work_dir="*.txt" label="Parsed text." />
    </outputs>

    <tests>
        <test>
           <param name="input_file" value="text.pdf"/>
           <output name="text" file="text.txt" compare="sim_size" delta="100"/>
        </test>
    </tests>

    <help><![CDATA[
Parse PDF
===================

Tool to parse PDF files.

    ]]></help>

    <citations>
        <citation type="doi">10.5281/zenodo.20036704</citation>
    </citations>

</tool>