<tool id="aoptk_download_publication_data" name="aoptk download publication data" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="25.1" license="MIT">
    <description>Download publication data - full text publications and figures, abstracts, or PDFs.</description>
    <macros>
        <import>macros.xml</import>
    </macros>

    <requirements>
        <expand macro="requirements"/>
    </requirements>
 
    <command detect_errors="exit_code"><![CDATA[
        python3 '${download_publication_data}'
    ]]></command>

<configfiles>
<configfile name="download_publication_data">
import os

with open("$input_file", "r") as f:
    ids = [line.strip() for line in f.readlines()]
email = os.environ.get("EMAIL")
api_key = os.environ.get("NCBI_API_KEY")


#if $data_type.type_choice == "publications":
from aoptk.literature.databases.pmc import PMC
from aoptk.literature.databases.europepmc import EuropePMC
from Bio import Entrez

if "${literature_database_europepmc_pmc}" == "pmc":
    Entrez.email = email
    Entrez.api_key = api_key
    pmc = PMC("", storage = "./", figure_storage="./figures")
    pmc.id_list = ids
    publications = pmc.get_publications()
elif "${literature_database_europepmc_pmc}" == "europepmc":
    europepmc = EuropePMC("", storage = "./", figure_storage="./figures")
    europepmc.id_list = ids
    publications = europepmc.get_publications()
else:
    raise ValueError("Select valid database.")

for publication in publications:
    with open(f"{publication.publication_id}.txt", "w") as f:
        f.write(publication.full_text)
#end if


#if $data_type.type_choice == "abstracts":
from aoptk.literature.databases.pubmed import PubMed
from aoptk.literature.databases.europepmc import EuropePMC
from Bio import Entrez

if "${literature_database_pubmed_europepmc}" == "pubmed":
    Entrez.email = email
    Entrez.api_key = api_key 
    pubmed = PubMed.__new__(PubMed)
    pubmed.id_list = ids
    abstracts = pubmed.get_abstracts()
elif "${literature_database_pubmed_europepmc}" == "europepmc":
    europepmc = EuropePMC("")
    europepmc.id_list = ids
    abstracts = europepmc.get_abstracts()
else:
    raise ValueError("Select valid database.")

for abstract in abstracts:
    with open(f"{abstract.publication_id}.txt", "w") as f:
        f.write(abstract.text)
#end if


#if $data_type.type_choice == "pdfs":
from aoptk.literature.databases.pmc import PMC

pmc = PMC("", storage = "./")
pmc.id_list = ids
pdfs = pmc.get_pdfs()
#end if

</configfile>
</configfiles>

    <inputs>
        <conditional name="data_type">
            <param name="type_choice" type="select" label="Data type">
                <option value="publications">Full text publications and figures</option>
                <option value="abstracts">Abstracts</option>
                <option value="pdfs">PDFs</option>
            </param>
        <when value="publications">
            <expand macro="literature_database_europepmc_pmc"/>
            <param name="input_file" type="data" format="txt" label="List of IDs to search for." help="Input text file with IDs to search for." />
        </when>
        <when value="abstracts">
            <expand macro="literature_database_pubmed_europepmc"/>
            <param name="input_file" type="data" format="txt" label="List of IDs to search for." help="Input text file with IDs to search for." />
        </when>
        <when value="pdfs">
            <param name="input_file" type="data" format="txt" label="List of IDs to search for." help="Input text file with IDs to search for." />
        </when>
        </conditional>
    </inputs>

    <outputs>
        <collection name="text" type="list" label="Downloaded publication text">
            <filter>data_type['type_choice'] == 'publications' or data_type['type_choice'] == 'abstracts'</filter>
            <discover_datasets pattern="(?P&lt;designation&gt;.*)\.txt$" format="txt" />
        </collection>
        <collection name="figures" type="list" label="Downloaded figures">
            <filter>data_type['type_choice'] == 'publications'</filter>
            <discover_datasets directory="figures" pattern="(?P&lt;designation&gt;.*)$" format="data" />
        </collection>
        <collection name="pdfs" type="list" label="Downloaded PDFs">
            <filter>data_type['type_choice'] == 'pdfs'</filter>
            <discover_datasets pattern="(?P&lt;designation&gt;.*)\.pdf$" format="pdf" />
        </collection>
    </outputs>

    <tests>
        <test>
            <conditional name="data_type">
                <param name="type_choice" value="publications"/>
                <param name="input_file" value="test-data/ids.txt"/>
                <output_collection name="text" type="list" count="2"/>
            </conditional>
            <conditional name="data_type">
                <param name="type_choice" value="abstracts"/>
                <param name="input_file" value="test-data/ids.txt"/>
                <output_collection name="text" type="list" count="2"/>
            </conditional>
            <conditional name="data_type">
                <param name="type_choice" value="pdfs"/>
                <param name="input_file" value="test-data/ids.txt"/>
                <output_collection name="pdfs" type="list" count="2"/>
            </conditional>
        </test>
    </tests>

    <help><![CDATA[
Download Publication Data
===================

Tools to download publication data - full text publications (including abstracts) and figures, abstracts, or PDFs.

    ]]></help>

</tool>